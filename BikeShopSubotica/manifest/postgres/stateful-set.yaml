apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-subotica
spec:
  serviceName: "subotica-db-service"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-subotica
  template:
    metadata:
      labels:
        app: postgres-subotica
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: subotica-configmap
              key: DB_USER
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: subotica-configmap
              key: DB_NAME
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: subotica-secret
              key: DB_PASSWORD
        volumeMounts:
        - name: postgres-subotica-storage
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: postgres-subotica-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi