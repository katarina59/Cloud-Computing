apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: central-ingress
  namespace: bike-shop
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: central.local
      http:
        paths:
          - path: /CentralBikeShop
            pathType: Prefix
            backend:
              service:
                name: central-app-service
                port:
                  number: 5000
